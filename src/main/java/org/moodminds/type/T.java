package org.moodminds.type;

/**
 * Generic object with one type parameter. Can be useful to transfer types when, for example,
 * it cannot be done with regular .class literal e.g. {@code Iterable<String>.class}.
 *
 * @param <V> the type of the parameter
 */
public class T<V> {}
